# Netlify 設定
# GitHub リポジトリを Netlify に接続すると、この設定でビルドされます。
# Next.js 用のプラグイン（@netlify/plugin-nextjs）は Netlify が自動で追加します。

[build]
  command = "npm run build"

[build.environment]
  # Node 22 = WebSocket などの標準APIが揃っているバージョン（Supabase クライアントに必要）
  NODE_VERSION = "22"
